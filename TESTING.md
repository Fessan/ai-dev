# Тестирование шаблона ai-dev

Инструкция как протестировать шаблон перед использованием в реальных проектах.

## Быстрый тест (5 минут)

### 1. Проверка структуры

```bash
PROJECT_DIR="/path/to/ai-dev"
cd "$PROJECT_DIR"

# Проверь что все файлы на месте
ls -la

# Должны быть:
# - INSTRUCTIONS.md
# - README.md
# - DECISIONS.md
# - .claude/
# - docs/
# - work/
# - tests/
```

### 2. Проверка guides

```bash
ls -la .claude/guides/

# Должно быть 5 файлов:
# - project.md
# - architecture.md
# - features.md
# - patterns.md
# - business-rules.md
```

### 3. Проверка команд

```bash
ls -la .claude/commands/

# Должно быть 9 файлов:
# - init-project.md
# - brainstorm.md
# - dev.md
# - review.md
# - plan.md
# - test.md
# - audit.md
# - secrets.md
# - update-guides.md
```

### 4. Проверка агентов

```bash
ls -la .claude/agents/

# Должно быть 4 файла:
# - code-developer.md
# - code-reviewer.md
# - security-auditor.md
# - secret-scanner.md
```

---

## Полный тест (30-60 минут)

### Тест 1: Чтение документации ИИ

**В Claude Code или через чат:**

```
Прочитай INSTRUCTIONS.md и расскажи что ты понял
```

**Ожидаемое поведение:**
- ИИ прочитает файл
- Объяснит основные правила
- Покажет понимание workflow

---

### Тест 2: Инициализация проекта

**Создай тестовую папку:**

```bash
PROJECT_DIR="/path/to/ai-dev"
mkdir /tmp/test-ai-dev

# Вариант A: чистая копия без .git (проверка "с нуля")
cp -r "$PROJECT_DIR"/* /tmp/test-ai-dev/

# Вариант B: копия с .git (проверка существующего репозитория)
# cp -r "$PROJECT_DIR"/.* /tmp/test-ai-dev/ 2>/dev/null || true

cd /tmp/test-ai-dev
```

**В Claude Code:**

```
/init-project
```

**Ответь на вопросы:**
- Название: TestBot
- Описание: Простой Telegram бот для тестирования
- Пользователь: Разработчики
- Проблема: Нужен бот для тестов
- Язык: Python
- Тип: Telegram бот
- Фреймворк: aiogram
- БД: SQLite
- Остальное: по умолчанию

**Что должно произойти:**
1. ИИ задаст ~10-15 вопросов
2. Заполнит все 5 guides в `.claude/guides/`
3. Создаст базовую структуру проекта
4. Создаст requirements.txt или package.json
5. Если `.git` отсутствовал — инициализирует git
6. Создаст коммит, если были изменения
7. Покажет команды для старта

**Проверка результата:**

```bash
# Проверь guides заполнены
cat .claude/guides/project.md
cat .claude/guides/architecture.md

# Если .git не было — git должен быть инициализирован
# Если .git уже был — /init-project не должен переинициализировать репозиторий
git log --oneline

# Должен появиться новый коммит с настройкой проекта (если были изменения)
```

---

### Тест 3: Brainstorming + создание фичи

**В Claude Code:**

```
Хочу добавить команду /start которая приветствует пользователя
```

**Что должно произойти:**
1. ИИ задаст уточняющие вопросы:
   - Какой текст приветствия?
   - Нужны ли кнопки?
   - Что-то ещё?

2. ИИ создаст:
   - `work/YYYY-MM-DD--start-command/prd.md`
   - `work/YYYY-MM-DD--start-command/plan.md`
   - `work/YYYY-MM-DD--start-command/tasks/task-001.md` и др.

3. ИИ попросит апрув для дальнейшей реализации

**Проверка:**

```bash
# Проверь что ПРД создан
ls -la work/

# Прочитай ПРД
cat work/*/prd.md

# Прочитай план
cat work/*/plan.md

# Должны быть acceptance criteria, задачи, детали
```

---

### Тест 4: Разработка задачи (TDD)

**После апрува плана:**

```
Да, начинай разработку первой задачи
```

или

```
/dev work/YYYY-MM-DD--start-command/tasks/task-001.md
```

**Что должно произойти:**
1. ИИ прочитает задачу, ПРД, guides
2. **TDD:** Напишет падающий тест ПЕРВЫМ
3. Запустит тест - убедится что падает (RED)
4. Напишет код
5. Запустит тест - убедится что проходит (GREEN)
6. Покажет отчёт
7. Спросит можно ли взять следующую задачу

**Проверка:**

```bash
# Проверь что тесты созданы
ls tests/

# Запусти тесты вручную
pytest tests/  # или npm test
```

**Важно проверить:**
- [ ] Тест написан ДО кода
- [ ] ИИ запустил тест и показал что он падает
- [ ] Код написан после теста
- [ ] ИИ запустил тест снова - он проходит
- [ ] Отчёт содержит путь к файлам, статус тестов

---

### Тест 5: Code Review

**После реализации:**

```
/review
```

**Что должно произойти:**
1. ИИ прочитает план, ПРД, изменённые файлы
2. Проверит соответствие плану
3. Проверит качество кода
4. Проверит тесты
5. Покажет отчёт с категориями:
   - 🔴 CRITICAL
   - 🟡 IMPORTANT
   - 🟢 SUGGESTIONS

**Проверка:**
- [ ] ИИ начал с позитивного фидбека
- [ ] Указал конкретные файлы и строки
- [ ] Приоритизировал проблемы
- [ ] Дал вердикт (APPROVED / CHANGES REQUESTED)

---

### Тест 6: Security Audit

```
/audit
```

**Что должно произойти:**
1. ИИ проверит код на OWASP Top 10
2. Поищет hardcoded secrets
3. Проверит валидацию входных данных
4. Покажет отчёт с уязвимостями

**Проверка:**
- [ ] Отчёт содержит проверку OWASP Top 10
- [ ] Если есть .env.example - проверил что нет реальных секретов
- [ ] Дал рекомендации по security

---

### Тест 7: Проверка секретов

**Создай файл с fake secret:**

```bash
echo 'API_KEY = "sk_test_fake123456789"' > test_config.py
git add test_config.py
```

**Запусти:**

```
/secrets
```

**Что должно произойти:**
1. ИИ найдёт потенциальный secret
2. Покажет где именно
3. Заблокирует коммит
4. Даст инструкции как исправить

**Проверка:**
- [ ] Secret обнаружен
- [ ] Файл и строка указаны
- [ ] Вердикт: COMMIT BLOCKED
- [ ] Даны рекомендации

**Очистка:**

```bash
git reset
rm test_config.py
```

---

### Тест 8: Автообновление guides

**После реализации фичи проверь:**

```bash
cat .claude/guides/features.md
cat .claude/guides/architecture.md
```

**Что должно быть:**
- В `features.md` добавлена новая фича
- В `architecture.md` обновлена структура (если менялась)
- В `DECISIONS.md` добавлены решения (если были)

---

### Тест 9: Универсальность (Codex CLI)

**Если есть доступ к Codex CLI:**

1. Открой проект в Codex
2. Попробуй: "Расскажи о проекте"

**Что должно произойти:**
- Codex прочитает `AGENTS.md` и `INSTRUCTIONS.md`
- Поймёт правила
- Прочитает guides
- Расскажет о проекте

**Проверка:**
- [ ] Codex следует тем же правилам
- [ ] Читает те же guides
- [ ] Понимает структуру

---

## Критерии успеха

### Шаблон работает если:

✅ **Инициализация:**
- `/init-project` задаёт вопросы и заполняет guides
- Создаётся рабочая структура проекта
- Git инициализируется автоматически, если `.git` отсутствует

✅ **Brainstorming:**
- ИИ задаёт уточняющие вопросы
- Не придумывает, всегда спрашивает
- Создаёт ПРД и план без апрува
- Просит апрув перед реализацией

✅ **TDD:**
- Тест ВСЕГДА пишется первым
- ИИ запускает тест и показывает RED
- Код пишется после теста
- ИИ запускает тест снова - GREEN

✅ **Автообновление:**
- Guides обновляются после изменений
- DECISIONS.md пополняется

✅ **Качество:**
- Code review даёт конкретные рекомендации
- Security audit находит уязвимости
- Secret scanner блокирует коммит с секретами

✅ **Универсальность:**
- Работает в Claude Code
- Работает в Codex CLI через `AGENTS.md` и `INSTRUCTIONS.md`

---

## Что делать если тест не прошёл

### ИИ не следует TDD

**Проблема:** ИИ пишет код перед тестом

**Решение:**
1. Проверь `.claude/guides/patterns.md` - там должно быть "TDD обязателен"
2. Напомни ИИ: "Сначала напиши падающий тест, потом код"
3. Если не помогает - добавь более явное правило в INSTRUCTIONS.md

### ИИ не спрашивает, придумывает

**Проблема:** ИИ делает предположения вместо вопросов

**Решение:**
1. Проверь INSTRUCTIONS.md - там должно быть "Запрещено предполагать"
2. Напомни: "Если непонятно - спроси, не придумывай"

### Guides не обновляются

**Проблема:** После изменений guides остаются старыми

**Решение:**
1. Проверь INSTRUCTIONS.md - там должно быть правило автообновления
2. Используй `/update-guides` вручную
3. Напомни ИИ: "Обнови guides после этого изменения"

### Команды не работают

**Проблема:** `/init-project` и другие команды не распознаются

**Решение:**
- В Claude Code команды работают как slash-команды
- В Codex CLI нужно говорить: "Выполни команду из .claude/commands/init-project.md"

---

## Финальный чеклист

Перед использованием шаблона в реальном проекте убедись:

- [ ] `/init-project` работает и заполняет guides
- [ ] Brainstorming задаёт вопросы, не придумывает
- [ ] TDD соблюдается (тест → код)
- [ ] Code review даёт полезный фидбек
- [ ] Security audit находит проблемы
- [ ] Secret scanner блокирует коммит с секретами
- [ ] Guides автоматически обновляются
- [ ] Работает в Claude Code
- [ ] Работает в Codex CLI (если есть доступ)

---

## Логирование проблем

Если нашёл проблему при тестировании:

1. Опиши что не работает
2. Что ожидал
3. Что получил
4. Какой файл проверял

Создай issue в репозитории или исправь сам.

---

**Happy testing!** 🧪
