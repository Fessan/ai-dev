# Инструкции для Codex CLI

Этот файл читается Codex CLI автоматически при старте сессии в репозитории.

## Обязательный порядок

1. Прочитай `INSTRUCTIONS.md` полностью.
2. Определи тип запроса и прочитай соответствующего агента:
   - разработка: `.claude/agents/code-developer.md`
   - ревью: `.claude/agents/code-reviewer.md`
   - аудит безопасности: `.claude/agents/security-auditor.md`
   - поиск секретов: `.claude/agents/secret-scanner.md`
3. Выбери минимум 1–2 релевантных skills из `.claude/skills/` и следуй им.
4. Всегда явно отображай какого агента вы используете.
5. Всегда явно отображай какого skills вы используете.

## Правила (sync from `rules/`)

Источник истины: `rules/core.md`, `rules/workflow.md`.

- Не делай предположений о стеке/командах без чтения файлов.
- Если данных не хватает — задай вопросы и дождись ответов.
- Перед любыми изменениями: мини-план + чек-лист + явный апрув.
- TDD обязателен: тест → запуск (RED) → код (GREEN) → запуск.
- После работы `code-developer` всегда запускай `code-reviewer`.
