# Project Context

> Заполняется при `/init-project`. Единый источник истины о проекте.

## Проект

**Название**: [название]

**Описание**: [что делает проект, 1-2 предложения]

**Пользователь**: [для кого]

## Стек

| Компонент | Технология |
|-----------|------------|
| Язык | [Python / TypeScript / Go / ...] |
| Фреймворк | [FastAPI / Express / aiogram / ...] |
| База данных | [PostgreSQL / MongoDB / нет] |
| Тесты | [pytest / jest / ...] |
| Линтер | [ruff / eslint / ...] |

## Структура

```
project/
├── src/           # Исходный код
├── tests/         # Тесты
└── ...
```

## Команды

```bash
# Установка
[команда]

# Запуск
[команда]

# Тесты
[команда]

# Линтер
[команда]
```

## Стиль кода (пример)

```python
# Показывай один реальный пример вместо описания
def get_user(user_id: int) -> User | None:
    """Получить пользователя по ID."""
    return db.query(User).filter_by(id=user_id).first()
```

## Особенности

- [Важные детали проекта]
- [Интеграции, ограничения]

---

**Автообновление**: ИИ обновляет этот файл при изменении стека, структуры или команд.
