Synced from rules/lite.md.

Quick Start:
1. Read context.md for stack, commands, structure
2. If unclear, ask and wait
3. Do

Boundaries:
✅ Always: read context.md, check existing code, validate inputs
⚠️ Ask first: config changes, adding dependencies, deleting files
🚫 Never: hardcode secrets, commit .env, assume stack

Rules:
- Simple task (1-3 files): clarify → code
- Complex task: mini-plan → approval → code
- Tests required for: payments, auth, user data

Additional context:
- .claude/agents/*.md — agents
- .claude/skills/*/SKILL.md — skills

Principles: KISS, YAGNI, DRY

Auto-update context.md after significant changes.
