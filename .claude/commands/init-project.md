# /init-project

Инициализация нового проекта через интервью.

## Описание

Эта команда проводит интервью (~10-15 вопросов) и заполняет все guides и структуру проекта.

## Процесс

### 1. Приветствие

```
Привет! Я помогу настроить новый проект.

Отвечай на вопросы, а я заполню guides и создам структуру.
Если не знаешь ответ - скажи, я предложу рекомендацию.

Поехали! 🚀
```

### 2. Вопросы о проекте

**1. Название проекта?**
```
Пример: TaskBot, MyAPI, WebApp
```

**2. Краткое описание проекта (1-2 предложения)?**
```
Что делает проект?
Пример: "Telegram бот для управления задачами и списками дел"
```

**3. Кто целевой пользователь?**
```
Для кого этот проект?
Пример: "Студенты и freelance разработчики"
```

**4. Какую главную проблему решает проект?**
```
Какая боль у пользователя?
Пример: "Сложно отслеживать задачи в разных проектах"
```

**5. Цели проекта (2-3 основные цели)?**
```
Что хочешь достичь?
```

### 3. Вопросы о технологиях

**6. Язык программирования?**
```
Варианты: Python, JavaScript/TypeScript, Go, другое
Рекомендация: Python (популярен, много библиотек, быстрая разработка)
```

**7. Тип проекта?**
```
Варианты:
a) Веб-приложение (backend API)
b) Telegram бот
c) CLI инструмент
d) Full-stack приложение
e) Другое

Рекомендация зависит от целей проекта.
```

**8. Фреймворк/библиотеки?** (зависит от ответа на #7)

Для веб (Python):
```
Варианты: FastAPI, Flask, Django
Рекомендация: FastAPI (современный, быстрый, автодокументация)
```

Для бота (Python):
```
Варианты: aiogram, python-telegram-bot, telebot
Рекомендация: aiogram (async, современный, хорошая документация)
```

Для веб (JS/TS):
```
Варианты: Express, NestJS, Fastify
Рекомендация: Express (простой, популярный) или NestJS (enterprise-ready)
```

**9. База данных?**
```
Нужна ли БД?
Варианты: PostgreSQL, MongoDB, SQLite, Не нужна
Рекомендация:
- PostgreSQL (если нужны ACID транзакции)
- MongoDB (если нужна гибкая схема)
- SQLite (для простых проектов)
```

**10. Дополнительные библиотеки/сервисы?**
```
Что ещё планируешь использовать?
Примеры: Redis, Celery, OAuth, Payment gateways
```

### 4. Вопросы о разработке

**11. Стиль коммитов?**
```
Варианты: Conventional Commits, свободный стиль
Рекомендация: Conventional Commits (стандартизация)
```

**12. Линтер/форматтер?**

Для Python:
```
Рекомендация: black (форматтер) + ruff (линтер)
```

Для JS/TS:
```
Рекомендация: prettier (форматтер) + eslint (линтер)
```

**13. Тестовый фреймворк?**

Для Python:
```
Рекомендация: pytest
```

Для JS/TS:
```
Рекомендация: jest или vitest
```

**14. Архитектурный паттерн?**
```
Варианты:
- Layered (простой: handlers → services → models)
- Clean Architecture (сложный, но масштабируемый)
- MVC (для full-stack)

Рекомендация: Layered (простой и понятный для старта)
```

**15. Особые бизнес-правила или требования?**
```
Есть ли специфичная бизнес-логика?
Примеры: "Пользователь может создать максимум 10 задач в день"
```

### 5. После ответов

```
Отлично! Создаю структуру проекта...
```

#### Создай:

1. **Заполни guides:**
   - `.claude/guides/project.md`
   - `.claude/guides/architecture.md`
   - `.claude/guides/features.md`
   - `.claude/guides/patterns.md`
   - `.claude/guides/business-rules.md`

2. **Создай базовую структуру кода** (зависит от типа проекта)

Python (backend/bot):
```
project/
├── src/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   └── [модули на основе архитектуры]
├── tests/
│   ├── __init__.py
│   └── test_main.py
├── requirements.txt
├── .env.example
└── README.md (project-specific)
```

JavaScript/TypeScript:
```
project/
├── src/
│   ├── index.ts
│   ├── config.ts
│   └── [модули]
├── tests/
│   └── index.test.ts
├── package.json
├── tsconfig.json
├── .env.example
└── README.md
```

3. **Создай конфигурационные файлы**

Python:
```python
# requirements.txt
# Базовые зависимости на основе ответов

# pyproject.toml или setup.py (если нужен)
```

JS/TS:
```json
// package.json
{
  "name": "[project-name]",
  "version": "0.1.0",
  ...
}
```

4. **Инициализируй git:**
```bash
git init
git add .
git commit -m "feat: initial project setup"
```

5. **Создай первый test:**

Python:
```python
# tests/test_main.py
def test_example():
    assert True, "Initial test passes"
```

JS/TS:
```typescript
// tests/index.test.ts
test('example', () => {
  expect(true).toBe(true);
});
```

### 6. Финальный отчёт

```markdown
✅ Проект инициализирован!

## Что создано

### Guides (база знаний)
- ✅ `.claude/guides/project.md`
- ✅ `.claude/guides/architecture.md`
- ✅ `.claude/guides/features.md`
- ✅ `.claude/guides/patterns.md`
- ✅ `.claude/guides/business-rules.md`

### Структура проекта
- ✅ `src/` - исходный код
- ✅ `tests/` - тесты
- ✅ Конфигурационные файлы
- ✅ `.env.example`
- ✅ `.gitignore`

### Git
- ✅ Репозиторий инициализирован
- ✅ Initial commit создан

## Следующие шаги

1. Установи зависимости:
   ```bash
   [команда установки зависимостей]
   ```

2. Скопируй .env.example в .env и заполни переменные:
   ```bash
   cp .env.example .env
   ```

3. Запусти тесты:
   ```bash
   [команда запуска тестов]
   ```

4. Начни разработку первой фичи:
   ```
   "Хочу добавить [название фичи]"
   ```

## Дополнительно

- Guides находятся в `.claude/guides/` - они будут автоматически обновляться
- INSTRUCTIONS.md - точка входа для ИИ, прочитай если интересно
- Следуй TDD: всегда пиши тест перед кодом

Готов к разработке! 🎉
```

## Принципы

- Дружелюбный тон
- Предлагай рекомендации если пользователь не знает
- Объясняй почему рекомендуешь конкретную технологию
- Создавай рабочий базовый проект, а не просто шаблон
- Первый тест должен проходить
